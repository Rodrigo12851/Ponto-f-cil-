import { Employee, DayPonto } from '../types';

// Blank initial employees - clean state for production / custom testing
export const INITIAL_EMPLOYEES: Employee[] = [];

// Empty sample employees array
export const SAMPLE_TEST_EMPLOYEES: Employee[] = [];
